
type HeaderInfo = {
    id? : number | null,
    name : string,
    introduction1? : string | null,
    introduction2? : string | null,
    imgSrc : string,
    lastModifiedAt? : Date,
    createdAt? : Date
}

export type {
    HeaderInfo
}
